package ru.job4j;


import org.junit.Test;

import java.util.*;

import static org.hamcrest.core.Is.is;
import static org.junit.Assert.assertThat;

public class ListsortTest {
/*    @Test
    public void sortList() {
        //List<String> data = new ArrayList<>();
        Set<List<String>> data = new TreeSet<>(new Comparator<List<String>>() {
            @Override
            public int compare(List<String> left, List<String> right) {
                return 1;
            }
        });
        //data.add(Arrays.asList()"2 2 3");
        //data.add("3 2 1");
        data.add(Arrays.asList("2", "2", "3"));
        data.add(Arrays.asList("1", "2", "3"));
        data.add(Arrays.asList("3", "2", "3"));
        //data.add(Arrays.asList("1 2 3"));
        //data.add(Arrays.asList( ("3 2 3")));
        Listsort sortlist = new Listsort();
        //List<String> result = sortlist.sortList(data);
        Set<List<String>> result = sortlist.addListToSet(data);
        assertThat(result, is(-1));
    }
*/
}
